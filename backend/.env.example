# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For local development, use:
# MONGODB_URI=mongodb://localhost:27017/health-prediction
# For production (MongoDB Atlas), use:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/health-prediction?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/health-prediction

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here_change_this_to_something_secure

# Hugging Face API Key (Optional - for enhanced ML predictions)
# Get your key from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Frontend URL (for CORS configuration)
# Local: http://localhost:3000
# Production: https://your-frontend-domain.vercel.app
FRONTEND_URL=http://localhost:3000